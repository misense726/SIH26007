# ESP32 firmware

The live firmware arrives after the simulated sensing-to-twin chain works. It will use staggered ToF acquisition and timestamp every sensor sample. See `docs/HARDWARE.md` for the frozen V1 layout.

